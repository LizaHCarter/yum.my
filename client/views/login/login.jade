.row
  .col-xs-3
    h3 {{title}}
    form(novalidate, name='userForm', role='form')
      .form-group
        label(for='email') Email
        input.form-control#email(name='email', type='email', ng-model='user.email', required, autofocus)
      .form-group
        label(for='password') Password
        input.form-control#password(name='password', type='password', ng-model='user.password', ng-minlength='3', required)
      .form-group
        button.btn.btn-primary(ng-show='userForm.$valid', ng-click='login()') Login!
